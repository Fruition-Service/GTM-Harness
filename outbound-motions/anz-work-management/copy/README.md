# copy/ — anz-work-management

## Reconciliation note (2026-08-05) — read this first for LinkedIn

`linkedin-sequence-champion.md` below was drafted independently, without
checking whether a live campaign already existed. One did:
**`512925 — "V3 - ANZ (WM)"` is `IN_PROGRESS` in HeyReach right now**, and
its real sequence is documented in
`knowledge/company/linkedin-messaging-foundation.md`. That's the copy
actually running — `linkedin-sequence-champion.md` is a divergent,
untested alternative, not a description of reality. Don't attach it to
`512925` or treat it as more authoritative than the live version. It may
still be useful as an alternate persona-specific variant to test
*against* the live one, but that's a deliberate A/B decision for Thana to
make, not something to assume.

The email side (`email-sequence-champion.md`) has no equivalent
contradiction found yet — Smartlead wasn't checked (no API key at time of
writing).

- [`email-sequence-champion.md`](email-sequence-champion.md) — 4-step
  Smartlead sequence, champion persona. Complete draft, not yet checked
  against a live Smartlead campaign.
- [`linkedin-sequence-champion.md`](linkedin-sequence-champion.md) —
  HeyReach connection + 2 messages, champion persona. Complete draft —
  **see reconciliation note above, a live sequence already exists.**
- [`spam-check-results.md`](spam-check-results.md) — manual pre-check;
  the real Smartlead spam-check tool run is still outstanding.

**Not yet written:** economic-buyer and influencer variants (see the TODO
at the bottom of `email-sequence-champion.md`) — needed before Tier A
multi-contact sends per `skills/list-segmentation/instructions.md` §3.

**Status: draft v1, unreviewed.** Everything here is a first pass through
`skills/campaign-copy/instructions.md` — it has not been through
`sops/persona-qa.md` sign-off and must not be attached to a live campaign
until it has.
