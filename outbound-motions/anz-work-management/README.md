# anz-work-management

Pilot motion (ANZ × Work Management) — the **first working slice** per
`AGENTS.md` §4. Prove the full loop here (ICP → list → copy → dedupe →
upload → report) before templatizing the remaining 8 motions.

Folder skeleton (identical for every motion, do not add ad hoc folders):

- `icp/` — avatar, lead-fit rubric, exclusions
- `research/` — evidence: proof catalogs, source registers, QA
- `lists/` — segmented lead buckets
- `copy/` — sequences, variants, spam-check results
- `campaigns/` — launch plans, live campaign state
- `reporting/` — weekly reports out
