# dashboard/ (optional)

Optional web app over campaign/pipeline state. Not required for the first
working slice (`AGENTS.md` §4) — build only once the pilot motion's loop
(ICP → list → copy → dedupe → upload → report) runs cleanly end-to-end.

<!-- TODO: scaffold once prioritized. Not started. -->
